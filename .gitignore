# Python
__pycache__/
*.pyc
*.pyo

# Virtual environment
.venv/

# Scratch / regenerable output -- all of these are produced by scripts/
# already in the repo, from data + decisions already recorded. Figures are
# not committed; regenerate with the commands in RUNNING.md. runs.csv IS
# committed (see below) -- the numbers, not the plots, are the record that
# must not drift from the code that produced them.
results/figs/
results/parallel_test/

# OS
.DS_Store
Thumbs.db
