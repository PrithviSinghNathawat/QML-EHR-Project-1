# Python
__pycache__/
*.pyc
*.pyo

# Virtual environment
.venv/

# Reversed 2026-08-18: results/figs/ was previously ignored (regenerable
# from committed scripts + data). The diagnostic session explicitly wants
# figures pushed alongside the report, so they're tracked now, including
# the ones from earlier sessions. Only true scratch output stays ignored.
results/parallel_test/

# OS
.DS_Store
Thumbs.db
